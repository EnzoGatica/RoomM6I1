<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/frag_agregar">

    <fragment
        android:id="@+id/frag_list"
        android:name="com.example.roomm6i1.frag_list"
        android:label="fragment_list"
        tools:layout="@layout/fragment_list" />
    <fragment
        android:id="@+id/frag_agregar"
        android:name="com.example.roomm6i1.Frag_agregar"
        android:label="fragment_agregar"
        tools:layout="@layout/fragment_agregar" >
        <action
            android:id="@+id/action_frag_agregar_to_frag_list"
            app:destination="@id/frag_list" />
    </fragment>
</navigation>